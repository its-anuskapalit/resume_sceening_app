# resume_sceening_app
 Machine Learning-powered application built with Streamlit that classifies resumes into job categories. It supports PDF, DOCX, and TXT formats, extracts text, cleans it, and predicts the category using an SVM classifier with TF-IDF vectorization. This tool is useful for HR teams, job portals, and career guidance applications
